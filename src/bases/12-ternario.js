const activo = true;

// const mensaje = (activo) ? 'activo' : 'incativo';

//Esto se funciona en el momento la condicion "activo" se cumpla,
//En caso contrario devuelve false 
const mensaje = activo && 'Activo' 

console.log(mensaje);